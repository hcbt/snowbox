# GPU passthrough is not a v1 plan

A Nix-built workstation might suggest CUDA/Metal in the Sandbox. Passthrough on Virtualization.framework is its own backend.

v1 is CPU. GPU is research later, not a promised capability. Limits and isolation claims do not mention a GPU.
