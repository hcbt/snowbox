# Theme and Mode belong to the Canvas

Theme (`twm` | `rio`) and Mode (`night` | `day`) are this Canvas’s chrome, stored with this browser. They are not Layout fields and not a Daemon API. A Canvas may Attach several Hosts; chrome that styles Icon Manager and every Window cannot live on one Host without picking a winner. Two browsers may disagree. Default is twm night.
