# A Sandbox is a Nix-built Linux system

Templates are flakes and the catalog is nixpkgs. A conventional distro with Nix on top is a second package manager. The Agent has passwordless sudo and will `apt install`; Reset then has two truths.

The Environment *is* the system: a Nix-built Linux rootfs. The user-facing word is not NixOS.
